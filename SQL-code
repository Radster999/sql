Here are the solutions to the queries numbered in the README file:

1. 
SELECT first_name, last_name, email
FROM customer;


2.
SELECT DISTINCT rating 
FROM film;


3. 
SELECT customer_id 
FROM payment
ORDER BY payment_date ASC
LIMIT 10;

4.
SELECT title, length 
FROM film
ORDER BY length ASC
LIMIT 5;

5.
SELECT COUNT(title) 
FROM film
WHERE length <= 50;

6.
SELECT COUNT (amount) FROM payment
WHERE amount > 5;

7.
SELECT COUNT (first_name) FROM actor
WHERE first_name LIKE 'P%';

8. 
SELECT COUNT(DISTINCT(district)) 
FROM address;

9.
SELECT COUNT(title) 
FROM film
WHERE title LIKE '%Truman%';

10.
SELECT staff_id, COUNT(amount)
FROM payment
GROUP BY staff_id;

11.
SELECT rating, ROUND(AVG(replacement_cost))
FROM film
GROUP BY rating;

